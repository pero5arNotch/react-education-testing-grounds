const STORE_ID = 'LOCATIONS' as const;

export const ADD = `${STORE_ID}/ADD` as const;
export const CLEAR = `${STORE_ID}/CLEAR` as const;
export const LOAD = `${STORE_ID}/LOAD` as const;
export const REMOVE = `${STORE_ID}/REMOVE` as const;
